{
  "extends": ["stylelint-config-tailwindcss"],
  "ignoreFiles": ["dist/**/*", "node_modules/**/*"],
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["plugin", "utility", "theme", "custom-variant"]
      }
    ]
  }
}
