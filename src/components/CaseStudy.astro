---
import { caseStudies } from "@/resources/case-studies";
import Detail from "./Detail.astro";

interface Props {
  label: string;
}

const { label } = Astro.props;
---

<Detail details={caseStudies} label={label} />
