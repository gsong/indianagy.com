---
import { caseStudies } from "@/resources/case-studies";
import Detail from "./Detail.astro";

interface Props {
  label: string;
}

const { label } = Astro.props;
---

<Detail category="Case Study" details={caseStudies} label={label} />
