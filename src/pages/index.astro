---
import Card from "@/components/Card.astro";
import Layout from "@/layouts/Layout.astro";
import { caseStudies } from "@/resources/case-studies";
import { expertise } from "@/resources/expertise";
---

<Layout>
  <h1 class="sr-only">India Nagy's Portfolio</h1>

  <aside class="mt-5 mb-10 text-xl font-semibold md:text-2xl">
    Art Director & Graphic Designer
  </aside>

  <section class="mb-10">
    <header class="header-content">
      <h2>Case Studies</h2>
      <p>
        Peek behind the scenes with these design case studies for beloved
        consumer brands.
      </p>
    </header>
    <ul class="grid grid-cols-1 gap-8 sm:grid-cols-2 md:grid-cols-3">
      {
        caseStudies.map(({ label, imgSrc, href }) => (
          <Card label={label} href={href} imgSrc={imgSrc} />
        ))
      }
    </ul>
  </section>

  <section class="mb-10">
    <h2>Expertise</h2>
    <ul class="grid grid-cols-1 gap-8 sm:grid-cols-2">
      {
        expertise.map(({ label, imgSrc, href }) => (
          <Card label={label} href={href} imgSrc={imgSrc} />
        ))
      }
    </ul>
  </section>
</Layout>
